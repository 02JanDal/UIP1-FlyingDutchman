/**
 * Created by Mumriken on 2016-02-03.
 */

$("document").ready(function() {

    // Append the element after all elements in the body. If there is no element it will be inserted (appended to the
    // empty element.ß
    //
    $("body").append("<p>The page just loaded!</p>");
});

